import 'package:flutter/material.dart';

class SafetyScreenc extends StatelessWidget {
  const SafetyScreenc({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
